*~
*.bak
*.swp
*.swo
tags
/.vscode
opentitan-docs

# Python
__pycache__
.venv/

# fusesoc result
build/

# build directories with special suffixes
build*/

# Generated register headers
hw/ip/*/sw

# JasperGold FPV Result
jgproject/

# CDC result
._dbname
._idebug_exit_script
idebugcfg.db
idebugcfg.db-journal
ivision.ws
meridian_project/
mrdc.his

# FPGA splice intermediate files
*.jou

# Simulation Results
*.log

# chip 实测证据要进 git：Ibex 侧的 UART 日志（8 测试 × 3 版本）。
# 其余 *.log（harness 的 run.log / sim.log / stall_profile 等）继续忽略。
# logs_hkem/ 下的实测日志**全部**要进 git（chip 的 uart0/sim、profiling 的 run/stall_profile 等）；
# 其余地方的 *.log 继续忽略（例如 test_perf/logs/ 的旧框架产物）。
!logs_hkem/**/*.log
# 例外：logs_hkem/<版本>/rtl/ —— **只有日志不提交，md 数据要提交**（方法 A 的产物）：
#   ✗ 不提交：*.rtl_trace.log（单文件百 MB 级）、*.uart0.log、*.sim.log、*.stdout.txt、*.json
#   ✓ 要提交：rtl_trace_<op>.md（逐函数表）、RTL实测_<版本>.md（该版本汇总）
# 写法说明：**不能直接排除目录**（父目录被排除后 `!` 救不回来）⇒ 排除内容、再放行 md。
logs_hkem/*/rtl/*
!logs_hkem/*/rtl/*.md
# 同上：logs_hkem/rtl_extra/ —— 补齐的 app（P-256 / HKDF）的原始 trace、ELF 与 JSON：
#   ✗ 不提交：*.rtl_trace.log（单文件几十 MB）、*.elf（bazel 可重建）、*.json、*.stdout.txt
#   ✓ 要提交：rtl_trace_<app>.md（逐函数表）
logs_hkem/rtl_extra/*
!logs_hkem/rtl_extra/*.md
vdCovLog/
INCA_libs/
*.shm/
.simvision/
*.history
irun.key
*.svcf
*.fsdb
*.rc
out/
scratch/
ucli.key
novas.conf
verdiLog/
ascent_project/
ascentlint.rpt
.idebugrc
idebugFiles/

# verilator/gtkwave waveforms and wave lists
*.fst
*.gtkw

# Foundry library
hw/foundry/

# ROM_EXT signer vendored in dependencies
sw/host/rom_ext_image_signer/vendored_dependencies

# Rust Cargo build system files.
sw/host/**/target
rust-project.json

# Bazel-related cache and output directories
bazel-*

# Site-specific bazel configuration
.bazelrc-site

# Compilation database generated from build system
compile_commands.json

# Local binaries that should not be committed to the codebase
.bin/

# Clangd index directory.
.cache/clangd/

# mdbook builds into `./book` directories, let's ignore them
book

# Hugo Lock
.hugo_build.lock

# Custom Ruff config overrides
.ruff.toml

# Synopsys Z01X configuration files
*.sff
hw/dv/tools/fi_sim.tcl
hw/dv/tools/dvsim/z01x.hjson
