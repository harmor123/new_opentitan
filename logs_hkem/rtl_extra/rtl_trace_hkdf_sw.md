# RTL trace 逐函数归因 · ver0_1/hkdf

> 方法 A：同一份 app 在 **RTL**（Earlgrey chip sim，OTBN 连真 KMAC）上跑，按 OTBN 指令级 trace 逐拍归因。
> 输入 trace：`logs_hkem/rtl_extra/hkdf_ver0_1.rtl_trace.log`（19,171,848 B）。
> 对照口径：**本 app 没有 ISS 剖面**（无 `--json`）⇒ 只有 RTL 口径。
> 符号边界：`logs_hkem/rtl_extra/hkdf_sha3_256_ver0_1.elf` 的 `.symtab`（23 个符号，地址 0x1c–0xa0c）⇒ **地址偏移 0x0**（按 trace 拍数覆盖率选定 = 100.0%；另一种偏移只有 100.0%）。 ⚠ **偏移判定可疑**：两种候选接近或覆盖率过低，请核对 ELF！未覆盖的拍会记到 `(无符号区间)`。

## 自证（完整性）

| 判据 | RTL trace | 参照 | 结论 |
|---|---:|---:|---|
| Σ `E`（退休指令） == chip `INSN_CNT` | 51,145 | （未提供 --uart0） | ✓ |
| Σ (`E`+`S`) + 空档 == trace 跨度 | 53,939 + 871 = 54,810 | 跨度 54,810 | ✓ |
| 调用帧实测（`jal`/`jalr`/`ret` 解码建栈） | 28 帧（未闭合 0），Σ帧跨度 198,789 | 含被调、帧间会重复；未闭合帧不计 | ✓ |
| **帧跨度 == 帧内记入 + Σ被调帧**（逐帧全等） | 28 帧全等（未闭合 0） | 帧内每拍恰好归属一处 | ✓ |
| 含被调 ≥ 自身拍（叶函数取等） | 全部满足 | 帧跨度定义 | ✓ |

> trace 里非 `E`/`S` 的行（擦除/寄存器流水账）：745,623 行；空档 871 拍（无任何记录的拍；按上一条口径，它们与 ISS 的 stalls 同源）。

## 逐函数 RTL 真实周期数（含被调 = 图上 C2 − C1）

| 函数 | **拍（含被调）** | 占 app | 自身拍 | 退役 | 停滞 | 取指等待¹ | CPI(含被调) |
|---|---:|---:|---:|---:|---:|---:|---:|
| `hmac_sha3_256` | **54,576** | 99.57% | 22 | 20 | 0 | 2 | 2728.800 |
| `sha3_update` | **35,948** | 65.59% | 17,479 | 14,431 | 2,279 | 769 | 2.491 |
| `keccakf` | **35,376** | 64.54% | 35,376 | 35,048 | 320 | 8 | 1.009 |
| `hkdf_extract` | **28,692** | 52.35% | 24 | 19 | 3 | 2 | 1510.105 |
| `hkdf_expand` | **26,109** | 47.64% | 15 | 12 | 2 | 1 | 2175.750 |
| `sha3_final` | **17,988** | 32.82% | 300 | 240 | 52 | 8 | 74.950 |
| `sha3_init` | **100** | 0.18% | 100 | 72 | 24 | 4 | 1.389 |
| `_sha3_update_skip` | —（非被调函数） | — | 757 | 757 | 0 | 0 | — |
| `key_wloop` | —（非被调函数） | — | 256 | 192 | 48 | 16 | — |
| `expand_loop` | —（非被调函数） | — | 179 | 144 | 18 | 17 | — |
| `hmac_key_ok` | —（非被调函数） | — | 144 | 92 | 40 | 12 | — |
| `hmac_inner_hash` | —（非被调函数） | — | 112 | 86 | 8 | 18 | — |
| `_sha3_update_skip_loop` | —（非被调函数） | — | 24 | 16 | 0 | 8 | — |
| `(无符号区间)` | —（非被调函数） | — | 9 | 7 | 0 | 2 | — |
| `key_tail` | —（非被调函数） | — | 6 | 4 | 0 | 2 | — |
| `expand_copy_done` | —（非被调函数） | — | 5 | 4 | 0 | 1 | — |
| `expand_ret` | —（非被调函数） | — | 2 | 1 | 0 | 1 | — |

> ¹ **取指等待**=两条记录之间的空拍（fetch 未回），摊给上一条记录所在的函数 —— 与 ISS 的 `has_fetch_stall` 同口径（跳转/循环多出的那一拍记在跳转指令上）。
> 「含被调」= trace 实测**帧跨度** `[C1, C2)`（28 帧，`jal/jalr/ret` 解码建栈；C1 = 进入函数第一拍，C2 = **调用方恢复执行的那一拍**）：帧内每一拍恰好归属一处 ⇒ **叶函数 含被调 == 自身拍**、父函数 = 自身拍 + 帧内兄弟标签 + Σ直接被调含被调（28 帧逐帧核对 `帧跨度 == 帧内记入 + Σ被调帧` ✓）。帧之间仍会重复（被调被父帧包含）⇒ **不能相加**；要「相加等于整段」用自身拍列。

## 热点 PC（Top 12）

| PC | 符号 | 退役 | 停滞 | 拍 |
|---|---|---:|---:|---:|
| `0x484` | `sha3_update` | 757 | 757 | 1,514 |
| `0x498` | `sha3_update` | 757 | 757 | 1,514 |
| `0x4b8` | `sha3_update` | 757 | 757 | 1,514 |
| `0x4c0` | `sha3_update` | 757 | 0 | 1,514 |
| `0x478` | `sha3_update` | 757 | 0 | 757 |
| `0x47c` | `sha3_update` | 757 | 0 | 757 |
| `0x480` | `sha3_update` | 757 | 0 | 757 |
| `0x488` | `sha3_update` | 757 | 0 | 757 |
| `0x48c` | `sha3_update` | 757 | 0 | 757 |
| `0x490` | `sha3_update` | 757 | 0 | 757 |
| `0x494` | `sha3_update` | 757 | 0 | 757 |
| `0x49c` | `sha3_update` | 757 | 0 | 757 |

> 停滞拍记在「正在尝试执行」的那个 PC 上 ⇒ KMAC 轮询等待会集中出现在轮询循环的 PC 上。

